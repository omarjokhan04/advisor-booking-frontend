import { Container } from "react-bootstrap";

export default function FindAppointments() {
  return (
    <Container className="py-4">
      <h1>Available Advisor Appointments</h1>
      <p className="text-muted">Next: filters + cards + Book button.</p>
    </Container>
  );
}
